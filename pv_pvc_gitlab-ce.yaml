apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-gitlab
spec:
  capacity:
    storage: 5Gi
    accessModes: 
      - ReadWriteOnce
    hostPath:
      path: /data/gitlab

-----------

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-gitlab
spec:
  accessModes:
    -ReadWriteOnce
  resources:
    requests:
      storage: 5Gi


